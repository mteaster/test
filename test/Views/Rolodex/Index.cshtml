@{
    ViewBag.Title = "Rolodex";
}

@section featured 
{
    @Html.Partial("_BandHeaderPartial")
}

<label for="Create">Create:</label>

<div>@Html.Action("Create Contact", "CreateContact", new { bandId = ViewBag.BandId })</div>

<br>

@{Html.RenderAction("RolodexList", "Rolodex", new { bandId = ViewBag.BandId });}